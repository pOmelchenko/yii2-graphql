# UID/GID of your host user so the container can create files with matching ownership.
HOST_UID=1000
HOST_GID=1000

# Enable Xdebug modes as needed. Valid values: off, debug, develop, coverage, profile, trace, gcstats (comma-separated).
XDEBUG_MODE=coverage

# Xdebug client configuration. Set host to your IDE listener (host.docker.internal works on Docker Desktop).
XDEBUG_CONFIG='client_host=host.docker.internal client_port=9003'
